#include "Header/Shader.h"

Shader::Shader()
{
	m_pProgram = NULL;
	m_vertex = NULL;
	m_fragment = NULL;
	m_geometry = NULL;
}

Shader::~Shader()
{

}

bool Shader::init(GLint* p_pProgram)
{
	m_pProgram = p_pProgram;
	if(m_pProgram == NULL)
		return false;
	return true;
}

bool Shader::createAndCompile(ShaderType p_shaderType, const char* p_pFileAddress)
{
	switch
	{
	}

	return true;
}
