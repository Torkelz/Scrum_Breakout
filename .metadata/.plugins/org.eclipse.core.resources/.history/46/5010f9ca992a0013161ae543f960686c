#version 400
layout (triangles) in;
layout (triangle_strip, max_vertices = 3) out;

in vec3 outputColor;
out vec3 colorGem;

void main()
{
	for (int i = 0; i < 3; i++)
	{
		gl_Position = gl_in[i].gl_Position;
		EmitVertex();
	}
	EndPrimitive();
	colorGem = vec3(1.0,0.0,0.0);
}