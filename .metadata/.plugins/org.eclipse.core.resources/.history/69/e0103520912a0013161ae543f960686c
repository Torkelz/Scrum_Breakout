#version 400
layout (points) in;
layout (triangle_strip) out;
layout (max_vertices = 4) out;

out vec2 texCoord;

void main()
{
	vec3 Pos = gl_in[0].gl_Position.xyz;
	
	vec3 toCamera = normalize(vec3(0.0, 0.0, 0.0) - Pos);
    vec3 up = vec3(0.0, 1.0, 0.0);
    vec3 right = cross(toCamera, up);
}